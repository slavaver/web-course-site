---
const { links } = Astro.props;
---

<ul class="p-2 bg-gray-200">
  {
    links.map((data) => (
      <li>
        <a href={data.url}>{data.frontmatter.shortTitle ? data.frontmatter.shortTitle : data.frontmatter.title}</a>
      </li>
    ))
  }
  <slot />
</ul>
